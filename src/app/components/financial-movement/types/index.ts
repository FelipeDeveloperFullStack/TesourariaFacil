interface IOut {
    description: string,
    currencyValue: string,
    month: Number
}

interface IIn {
    name: string,
    description: string,
    currencyValue: string,
    month: Number
}

export type { IOut, IIn } 